/*Using the function you have created in previous question,
 call the function multiple times with different parameters.
*/


function double(paisa){
    let answer = paisa * 2;
    return answer;
}
// calling function
console.log(double(5)); // printing answer
double(23);
double(67); // you can check them in console log
